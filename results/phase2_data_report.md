# Phase 2 data report: XboxSupport

_Generated by `python -m src.dataprep.build` from `data/raw/twcs.csv`. Numbers are regenerated on each run; the notes block is hand-written and kept._

<!-- manual:start -->
## Notes (hand-written)

**Exit criteria**

| Criterion | Status |
|---|---|
| Canonical `records.parquet` | 18,712 exchanges |
| No thread in both train and holdout | Met; tested in `tests/test_split_integrity.py` |
| Split-reply merging verified | 3,604 usable replies merged from 2+ tweets; 9 (0.25%) still out of marker order |
| 20+ reconstructed cases inspected by hand | 36 of 36 correct in the second round; see [`reconstruction_samples.md`](reconstruction_samples.md) |
| Counts match the brand-validation funnel | 18,712 vs 18,549 (+0.9%) |

The extra 163 come from merging split customer messages: about 158 messages pass the 3-word
filter only once their parts are joined.

**What moved against the Phase 1 numbers, and why**
- **Substantive replies: 3,989 (Phase 1: 4,413).** The rule is stricter: a DM request that
  mentions "try" is a deflection, not guidance.
- **Follow-ups: 4,908 (Phase 1: 5,543).** Replies to brand announcements are now first contacts.

**Splits.** `split_date` is 2017-11-15:

| Split | Exchanges | Note |
|---|---|---|
| train | 12,792 | 68.8% of train + holdout |
| holdout | 5,803 | 5,798 eval-eligible |
| excluded | 117 | Written after the split date in threads that started before it |

The holdout covers the final ~2.5 weeks, so some topic drift between train and evaluation is real.

**Leakage**
- Only 5 holdout customer messages have a near-duplicate in train.
- Reply templates are a different story. 690 of 1,331 holdout DM deflections and 752 of 3,223
  holdout "other" replies reuse a template that also occurs in train, against only 66 of 1,249
  substantive replies.
- A reply judged against a canned reference can therefore score well by copying. Phase 9 must
  slice reply quality by `reply_template_in_train`.

**Weak outcomes.** 86% of train exchanges are `deflected` or `unknown`. Only 307 are `resolved`,
and just 116 of those with medium confidence. This confirms the Phase 1 decision: outcomes only
nudge retrieval re-ranking and are never an evaluation target.

**Known limits**
- `substantive` is a retrieval heuristic, not ground truth: its precision is about 60% in the
  hand sample. It gates `retrieval_eligible` and nothing else.
- Escalation cues are keyword heuristics.
- 56 context brand turns are still fragments.
- The language filter is an estimate, not a classifier.
<!-- manual:end -->

## 1. Funnel (checked against Phase 1)

| step | phase 2 | phase 1 (brand_validation.md) | difference |
|---|---|---|---|
| customer messages with a brand reply | 20213 | 20213 | 0 |
| drop non-English | 20004 | 20008 | -4 |
| drop < 3 words after removing handles/URLs | 18899 | 18741 | 158 |
| drop DM hand-off messages ("DM sent") | 18851 | 18692 | 159 |
| drop exact-duplicate customer texts (earliest kept) | 18712 | 18549 | 163 |

Phase 2 also merges split customer messages, so a few merged messages pass the word filter that failed as single tweets, and duplicates are judged on the merged text.

- Brand replies merged from 2+ tweets: 3,938 of 20,213 exchanges (3,604 usable).
- Customer messages merged from 2+ tweets: 1,179 usable.
- Follow-ups: 4,908, of which 42 have no context left (their earlier turns were more than 7 days old).
- Contexts cut (more than 3 turns, or stale turns dropped): 2,287.

## 2. Splits

`data.split_date` = 2017-11-15. Threads starting on/after it → `holdout`; earlier threads → `train`, except exchanges written on/after it → `excluded`.

| split | exchanges | threads | first_contact | followup | substantive | dm_deflection | other | retrieval_eligible | eval_eligible | first_day | last_day |
|---|---|---|---|---|---|---|---|---|---|---|---|
| excluded | 117 | 73 | 28 | 89 | 29 | 21 | 67 | 0 | 0 | 2017-11-15 | 2017-12-03 |
| holdout | 5803 | 3913 | 4202 | 1601 | 1249 | 1331 | 3223 | 0 | 5798 | 2017-11-15 | 2017-12-03 |
| train | 12792 | 8878 | 9574 | 3218 | 2711 | 2993 | 7088 | 2711 | 0 | 2014-01-29 | 2017-11-14 |

- Threads in both train and holdout: **0**
- Every train exchange was written before the split date and every holdout thread starts on or after it: **yes**
- Excluded: 117 exchanges (73 threads) written after the split date in threads that started before it.
- Train share of train + holdout: 68.8%

## 3. Leakage checks (holdout vs train)

- Holdout messages with a train near-duplicate (cosine ≥ 0.95): **5**. These are not `eval_eligible`.
- For reference: ≥ 0.9: 8; ≥ 0.8: 35; median: 0.35.
- Holdout replies whose template also occurs in train (a retriever could copy them; reply-quality results on these must be read with care):

| reply_type | template_also_in_train | holdout_exchanges |
|---|---|---|
| dm_deflection | 690 | 1331 |
| other | 753 | 3223 |
| substantive | 66 | 1249 |

## 4. Weak outcomes (train only)

| weak_outcome | high | low | medium | none |
|---|---|---|---|---|
| acknowledged | 0 | 704 | 0 | 0 |
| deflected | 2799 | 0 | 0 | 0 |
| resolved | 0 | 191 | 116 | 0 |
| unknown | 0 | 0 | 0 | 8228 |
| unresolved | 0 | 0 | 754 | 0 |

| weak_outcome | rule | outcome_confidence | basis |
|---|---|---|---|
| resolved | customer confirms a fix after a substantive reply | medium | fixes are real, but only ~half credit the brand's reply |
| resolved | customer confirms a fix after any other reply | low | mostly fixed elsewhere (chat, phone, by themselves) |
| unresolved | still / not working / negated fix ("none of these worked") | medium | ~2/3 of sampled cues were genuine |
| acknowledged | thanks without a fix cue | low | ~2/12 samples mentioned a fix |
| deflected | no cue in the answer (or no answer) and the reply was a pure DM request | high | the deflection itself is observed; DM detection was right in every sample |
| unknown | no answer or an answer with no cue | none | nothing to go on |

## 5. Escalation signals

Customer-side cues (all splits; derived from the model's input):

| code | exchanges |
|---|---|
| prior_contact | 2042 |
| repeat_contact_cue | 2030 |
| anger | 943 |
| billing_dispute | 396 |
| security | 97 |
| legal_threat | 10 |

Brand-side evidence (train only; describes what the historical reply did):

| code | exchanges |
|---|---|
| dm_request | 3171 |
| handoff_other_channel | 884 |
| policy_enforcement | 349 |

## 6. Cleaning decisions

1. **Scope.** Only exchanges where the configured brand replied to a customer tweet. Brand tweets that answer nobody (announcements) are ignored.
2. **Split brand replies are one response.** A brand tweet answering the brand's own reply within 15 min is a continuation (`1/2`, `2/2`, trailing ` 1`). Every brand tweet answering the same customer tweet is merged in time order, up to 5 tweets.
3. **Split customer messages are one message.** The customer's own tweets directly before the answered tweet (each within 15 min) are merged into it, unless the brand answered the earlier tweet separately.
4. **Context for follow-ups.** The parent chain before the message becomes logical turns (consecutive tweets by one author = one turn). A brand turn shows the brand's whole merged reply, even if the customer answered only one of its parts. Threads with more than 3 earlier turns keep the opener and the last 2, so the original issue survives. Turns more than 7 days older than the message (revived old threads) are dropped. Both cuts set `context_truncated`.
5. **Follow-up vs first contact.** `is_followup` = the message answers an earlier brand *reply*. Answering a brand announcement (a brand tweet that replies to nobody) is a first contact, though the announcement still appears as context.
6. **Dropped: non-English messages** (function-word estimate; see `src/dataprep/text.py`). `undetermined` messages are kept if they pass the next filter.
7. **Dropped: messages with < 3 alphabetic words** after removing handles and URLs ("Yes", "Done", bare links). They state no issue.
8. **Dropped: DM hand-off messages** (< 8 words mentioning DM/sent, e.g. "DM sent").
9. **Dropped: exact duplicates** of the customer message (letters only, case-folded). The earliest copy is kept.
10. **Clean text keeps the raw text next to it.** Clean text: HTML entities decoded (`&amp;` → `&`), leading @handles removed, other handles → `@user`, URLs → `<URL>`. Brand text also loses agent sign-offs (`^XS`) and split markers. Emoji and casing are kept.
11. **reply_type.** `substantive` = a troubleshooting/guidance cue in a sentence that is not itself a DM request, and the reply is not wholly canned. `dm_deflection` = a DM request with no such guidance. `other` = the rest (clarifying questions, hand-offs, info, pleasantries, canned guidance). This is stricter than Phase 1, whose rule counted "Can you DM us what you see when you try …" as substantive: a hand check found about 70% of the 488 substantive-plus-DM replies were really DM requests. Phrases describing the customer's own attempts ("when you try", "you've tried") are not guidance.
12. **Grounding corpus.** Only `retrieval_eligible` rows (train + substantive) may enter the retrieval index. DM deflections and `other` replies never do.
13. **Split per thread, by time.** A thread whose root tweet is on/after `data.split_date` goes wholly to `holdout`. An earlier thread goes to `train`, except for exchanges written on/after the split date (revived threads). Those are `excluded` from both, so every train exchange strictly precedes every holdout thread and no thread is in both train and holdout.
14. **Leakage.** Holdout messages with a train near-duplicate (char TF-IDF cosine ≥ 0.95) are not `eval_eligible`. Holdout replies whose template also occurs in train are flagged (`reply_template_in_train`).
15. **Weak labels stay in train.** `weak_outcome`, `outcome_confidence`, `next_customer_text` and `brand_escalation_evidence` come from the future or from the reference reply, so they are null outside train. `customer_escalation_signals` come from the model's input and are kept everywhere.
16. **No intent labels yet.** The Phase 1 keyword draft intents are not stored: 42% of messages match none, mostly vague requests and follow-ups that need context. Intents come from the hand-written codebook in Phase 3.

## 7. Data dictionary (`data/processed/records.parquet`)

| column | meaning |
|---|---|
| `record_id` | Tweet ID of the customer tweet the brand answered |
| `thread_id` | Tweet ID of the thread's root. Splits are assigned per thread |
| `thread_started_at` | Time of the thread's root tweet (UTC). Decides the split |
| `split` | `train` (thread and exchange before `data.split_date`), `holdout` (thread started on/after it; Phase 5 samples dev/golden from it) or `excluded` (written after the split date in a thread that started before it) |
| `created_at` | Time of the customer tweet (UTC) |
| `customer_id` | Anonymised customer ID |
| `customer_prior_threads` | Earlier threads this customer had with the brand |
| `is_followup` | The message answers an earlier brand tweet (`False` = first contact) |
| `context` | Earlier turns as `[{role, text, created_at}]`, oldest first. Roles: customer, brand, other_customer, other_brand |
| `context_turns` | Number of context turns kept |
| `context_truncated` | Context was cut: more than 3 earlier turns (kept the opener and the last 2) and/or turns older than 7 days dropped |
| `customer_tweet_ids` | Customer tweets merged into the message |
| `n_customer_parts` | How many customer tweets were merged |
| `customer_text` | Customer message, raw |
| `customer_text_clean` | Customer message for models: entities decoded, handles → `@user`, URLs → `<URL>` |
| `lang` | Estimated language of the message (`en` or `undetermined`; `other` is dropped) |
| `n_words` | Alphabetic words in the message (handles and URLs excluded) |
| `customer_escalation_signals` | Weak cues in the message: anger, legal_threat, security, billing_dispute, repeat_contact_cue, prior_contact |
| `reply_tweet_ids` | Brand tweets merged into the reply |
| `n_reply_parts` | How many brand tweets were merged (split replies) |
| `brand_reply` | Brand reply, raw |
| `brand_reply_clean` | Brand reply without handles, agent sign-off (`^XS`) or split markers. URLs → `<URL>` |
| `reply_created_at` | Time of the reply's first tweet (UTC) |
| `reply_delay_min` | Minutes from the customer tweet to the reply |
| `reply_type` | `substantive` (actionable, not canned), `dm_deflection` (DM request, no guidance) or `other` |
| `reply_is_canned` | Every part of the reply is a template the brand used ≥ 5 times |
| `reply_template` | Normalised template of the reply, for the canned and leakage checks |
| `retrieval_eligible` | May enter the grounding/RAG corpus: train split **and** substantive reply |
| `max_train_similarity` | Holdout only: highest TF-IDF cosine between this message and any train message |
| `eval_eligible` | Holdout only: may be sampled into dev/golden (no train near-duplicate ≥ 0.95) |
| `reply_template_in_train` | Holdout only: the same reply template also occurs in train |
| `next_customer_text` | Train only: the same customer's first answer to the reply |
| `weak_outcome` | Train only: resolved / unresolved / acknowledged / deflected / unknown (heuristic) |
| `outcome_confidence` | Train only: high / medium / low / none. How far to trust `weak_outcome` |
| `weak_outcome_source` | Train only: `heuristic_v1` |
| `brand_escalation_evidence` | Train only: what the historical reply did: dm_request, handoff_other_channel, policy_enforcement (weak) |
